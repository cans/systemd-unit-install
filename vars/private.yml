---
_systemd_unit_install_state_map:
  disabled:
    _state: "{{omit}}"
    _enabled: "false"
  enabled:
    _state: "stopped"
    _enabled: "true"
  started:
    _state: "started"
    _enabled: "true"
_systemd_unit_install_unit_descriptor_defaults:
  tasks: [false]

# vim: et:syntax=yaml:sw=2:ts=2:
